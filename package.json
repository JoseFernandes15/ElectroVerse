{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "serve": "php artisan serve",
        "seed": "php artisan db:seed",
        "clear": "php artisan optimize",
        "migrate": "php artisan migrate",
        "rollback": "php artisan migrate:rollback",
        "test": "php artisan test",
        "restart": "php artisan migrate:rollback && php artisan migrate && php artisan db:seed"
        },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
